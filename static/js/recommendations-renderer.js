function renderRecommendations(){const rawRecommendations=window.__JINJA_AI_RECOMMENDATIONS__;const container=document.getElementById('recommendationsContent');if(!rawRecommendations||!container)return;try{const recommendations=typeof rawRecommendations==='string'?JSON.parse(rawRecommendations):rawRecommendations;if(recommendations.error){container.innerHTML=`<div class="rec-error"><div class="rec-error-title">⚠️ ${recommendations.error}</div><div>${recommendations.message}</div></div>`;return;}let html='';if(recommendations.trend_status){const statusClass=`rec-status-${recommendations.trend_status.toLowerCase()}`;const statusText=recommendations.trend_status.toUpperCase();html+=`<div class="rec-status-badge ${statusClass}">📊 Status: ${statusText}</div>`;}if(recommendations.executive_summary){html+=`<div class="rec-summary">💡 ${recommendations.executive_summary}</div>`;}if(recommendations.spending_insights&&recommendations.spending_insights.length>0){html+='<div class="rec-section-header">Chart Analysis & Insights</div>';recommendations.spending_insights.forEach(insight=>{html+=`<div class="rec-insight-card"><div class="rec-insight-title">📈 ${insight.title}</div><div class="rec-insight-text">${insight.finding}</div><div class="rec-insight-implication">💭 ${insight.implication}</div></div>`;});}if(recommendations.action_items&&recommendations.action_items.length>0){html+='<div class="rec-section-header">Recommended Actions</div>';recommendations.action_items.forEach(action=>{const priorityClass=`rec-action-priority-${action.priority.toLowerCase()}`;html+=`<div class="rec-action-item"><span class="rec-action-priority ${priorityClass}">${action.priority} Priority</span><div class="rec-action-category">🎯 ${action.category}</div><div class="rec-action-amounts"><div class="rec-action-amount"><span class="rec-action-amount-label">Current:</span><span class="rec-action-amount-value">${action.current}</span></div><div class="rec-action-amount"><span class="rec-action-amount-label">Savings:</span><span class="rec-action-amount-value" style="color: #22c55e;">+${action.potential_savings}</span></div></div><div class="rec-action-description">✓ ${action.recommendation}</div></div>`;});}if(recommendations.risk_assessment){const riskLevel=recommendations.risk_assessment.level||'LOW';const riskClass=`rec-risk-level ${riskLevel.toLowerCase()}`;html+=`<div class="rec-section-header">Risk Assessment</div><div class="rec-risk-section"><div class="${riskClass}">Risk Level: ${riskLevel}</div>`;if(recommendations.risk_assessment.top_concerns&&recommendations.risk_assessment.top_concerns.length>0){html+='<ul class="rec-risk-concerns">';recommendations.risk_assessment.top_concerns.forEach(concern=>{html+=`<li>${concern}</li>`;});html+='</ul>';}if(recommendations.risk_assessment.mitigation_strategy){html+=`<div class="rec-risk-mitigation"><strong>Mitigation Strategy:</strong><div>${recommendations.risk_assessment.mitigation_strategy}</div></div>`;}html+='</div>';}if(recommendations.budget_optimization){html+=`<div class="rec-section-header">Budget Optimization</div><div class="rec-budget-grid"><div class="rec-budget-item"><div class="rec-budget-label">Fixed Costs</div><div class="rec-budget-value">${recommendations.budget_optimization.recommended_fixed_percentage}</div></div><div class="rec-budget-item"><div class="rec-budget-label">Variable Costs</div><div class="rec-budget-value">${recommendations.budget_optimization.recommended_variable_percentage}</div></div></div><div class="rec-budget-grid"><div class="rec-budget-item" style="grid-column: 1 / -1;"><div class="rec-budget-label">Monthly Savings Goal</div><div class="rec-budget-value" style="color: #22c55e;">${recommendations.budget_optimization.monthly_savings_goal}</div></div></div>`;if(recommendations.budget_optimization.rationale){html+=`<div class="rec-budget-rationale"><strong>Why This Works:</strong> ${recommendations.budget_optimization.rationale}</div>`}}container.innerHTML=html;}catch(error){console.error('Error rendering recommendations:',error);container.innerHTML=`<div class="rec-error"><div class="rec-error-title">Error Rendering Recommendations</div><div>Could not display AI analysis.</div></div>`;};}if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',renderRecommendations);}else{renderRecommendations();}